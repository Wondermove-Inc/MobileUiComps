import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  indicator_container: {
    flexDirection: 'row',
    justifyContent: 'center',
    marginTop: 15,
  },

  indicator_style: {
    width: 12,
    height: 12,
    borderRadius: 12 / 2,
  },
});
