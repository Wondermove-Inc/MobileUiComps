import {StyleSheet} from 'react-native';
import {width, height} from '@styles';

export default StyleSheet.create({
  tiny_spinner: {
    width: width.spinner_tiny,
    height: height.spinner_tiny,
  },
  small_spinner: {
    width: width.spinner_small,
    height: height.spinner_small,
  },
  medium_spinner: {
    width: width.spinner_medium,
    height: height.spinner_medium,
  },
  large_spinner: {
    width: width.spinner_large,
    height: height.spinner_large,
  },
  giant_spinner: {
    width: width.spinner_giant,
    height: height.spinner_giant,
  },
});
