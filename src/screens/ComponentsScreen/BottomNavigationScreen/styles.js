import {StyleSheet} from 'react-native';
import {colors, font, fontColors, fontSizes} from '@styles';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 20,
  },
  title_text: {
    marginVertical: 20,
    marginStart: 20,
  },
});
